# パッチ更新の使い方

## 1. 最新パッチを取得する

Macの「ターミナル」に次の1行を貼り付け、Enterを押します。追加ライブラリのインストールは不要です（Python 3.9以降・Git・インターネット接続が必要）。

```sh
cd ~/OW-Counter-Assistant-Public && python3 update_overwatch.py
```

バックアップ → Blizzard英語公式の最新ライブパッチ取得 → 変更ヒーロー抽出 → 相性提案作成 → 構文・回帰テスト → ローカルコミットまで実行します。取得日と公式の日付は時差で異なる場合があります。同じパッチの再取得では既存レビューを上書きしません。パッチは最新の1記事を対象とし、長期間更新していなかった場合の過去パッチの累積適用は行いません。

## 2. 提案を確認する

`updates/latest.json` に最新フォルダ名があります。そのフォルダの `REVIEW.md` にヒーロー一覧、`patch.json` にURL・取得日時（UTC）・対象日付・変更原文、`source.html` に取得したページ、`proposal.json` に候補を保存しています。スタジアム専用・不具合修正の項目は一覧に記録し、通常対戦の数値候補には含めません。未知の新ヒーローは「未登録」として表示され、手動でデータ追加が必要です。

相性値はBlizzardが発表した値ではありません。**初期の proposed は現在値の据え置き**です。`suggested_range` は確認用の±1の範囲で、強化・弱体化の断定ではありません。5v5/6v6限定、距離、アビリティ、双方の変更を原文で確認してください。候補には同じ組み合わせが複数ファイルに存在します。D.Mon専用データや説明用の旧データも含むため、同じ対戦の関連行を合わせて確認してください。

採用する行だけ `proposed`（0〜10の整数、低いほど有利）を修正し、`approved` を `false` から `true` に変更して保存します。その他の項目は変更しません。例：現在6を5に変えるなら `"proposed": 5, "approved": true`。JSONでは末尾の余分なカンマに注意してください。未承認行は反映されません。据え置きだけの場合は承認を実行する必要はありません。

## 3. 確認した値を反映する

```sh
cd ~/OW-Counter-Assistant-Public && python3 update_overwatch.py --approve
```

再バックアップ → 承認行の数値だけ置換 → テスト → 成功時のみコミットします。テスト失敗時は相性ファイルを元に戻し、編集済み提案は残します。承認済みの再実行や古いデータへの反映を拒否します。**pushはどちらのコマンドも実行しません。** 公開サイトはまだ更新されません。

## 困ったとき

- 未コミットの変更：他の作業を先に保存・コミットしてください。勝手な削除や上書きは行いません。承認時だけ proposal.json の編集を許可します。
- 接続・HTML解析失敗：時間をおいて再実行。公式ページの形式変更時はスクリプト修正が必要です。
- 採用する変更がない：数値据え置きならそのままで完了です。変更する場合のみ proposed と approved を編集します。
- Gitの名前・メール未設定：Gitから表示される設定案内に従ってください。個人情報は自動設定しません。
- テストだけ実行：`python3 update_overwatch.py --check`
- バックアップ：隠しフォルダ `.update_backups/日時-ID/`。追跡ファイルのコピーと元のコミット番号を保持します。Finderでは Command + Shift + . で隠しファイルを表示できます。
- 更新が中断して `.update.lock` が残った場合：他の更新が動いていないことを確認して、この空フォルダだけ削除してください。
- 初回取得がコミット前に失敗し `updates` が残った場合：内容とテストを確認して保存・コミットしてから再実行してください。

## 検証範囲

全Pythonファイルの構文確認、全ヒーローの危険度・敵0人・ミラー、固定乱数の1,000編成×3ロール、更新スクリプトの解析・値検証・古い提案拒否・失敗時復元を検証します。app.pyと画面デザインは更新処理で書き換えません。ブラウザ上の外観確認や実戦相性の妥当性までは自動テストでは保証しません。
